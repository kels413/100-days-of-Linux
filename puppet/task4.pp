/*
Parameterize Content:

Extend the manifest to allow the content of the file to be specified as a parameter. This way, you can easily change the content without modifying the manifest.
*/


file{
    ensure => present,
}